// this file uses repeatable functions or displays such as a back button to a easy function that
// will return the required details so we can just call the function.
mod back_button;

pub use back_button::BackButton;
